kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
  - role: worker

# if planning to use load balancer in the cluster, follow https://github.com/kubernetes-sigs/cloud-provider-kind